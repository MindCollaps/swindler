# JWT Authentication
# Note: NITRO_JWT_SECRET is deprecated. The app now uses RSA key pairs stored as .pem files
# If you need custom key paths, use NITRO_JWT_PRIVATE_KEY_PATH and NITRO_JWT_PUBLIC_KEY_PATH
NITRO_JWT_SECRET=

# Admin User Configuration
# IMPORTANT: Use strong, unique passwords for admin accounts
ADMIN_USERNAME=
ADMIN_PASSWORD=
ADMIN_EMAIL=

# Database Configuration
# Replace POSTGRES_USER and POSTGRES_PASSWORD with your actual credentials
DATABASE_URL=postgresql://POSTGRES_USER:POSTGRES_PASSWORD@db:5432/swindler

# PostgreSQL Configuration
# IMPORTANT: Use strong, unique passwords in production
POSTGRES_USER=xxx
POSTGRES_PASSWORD=xxx
POSTGRES_DB=swindler-db

# Redis Configuration
REDIS_HOST=redis
# IMPORTANT: Use a strong password for Redis in production
REDIS_PASSWORD=xxx

# CORS Configuration (optional)
# Comma-separated list of allowed origins for Socket.io connections
# Example: ALLOWED_ORIGINS=https://example.com,https://app.example.com
# Leave empty or use '*' to allow all origins (not recommended for production)
ALLOWED_ORIGINS=